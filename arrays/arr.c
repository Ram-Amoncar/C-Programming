// To write program to understand array.
// Initialization of array
#include <stdio.h>

int main()
{
    int arr[5] = {10, 20, 30, 40, 50};

    printf("The elements of the array are:");
    for (int i = 0; i < 5; i++)
    {
        printf(" %d ", arr[i]);
    }
    return 0;
}
